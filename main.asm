org 0x7C00
bits 16
;; 0x7C00 is the address where the BIOS loads the boot sector
;; into memory. The BIOS does not care what is in the boot
;; sector, so we can do whatever we want.

;; FOR NOW I DON'T REALLY KNOW ANYTHING ABOUT THE ASSEMBLY SHIT... I'm trying to learn some